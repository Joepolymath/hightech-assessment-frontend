import ContentHeader from './ContentHeader';

const Main = () => {
  return (
    <main className="flex flex-col w-full bg-dark-600 h-full">
      <ContentHeader />
    </main>
  );
};

export default Main;
